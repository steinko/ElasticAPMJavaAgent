image: ubuntu
stages:
  - build
 


build-backend:
  stage: build
  image: java:11
  script:
    - ./postgressService/gradlew build 
  artifacts:
    paths:
      - ./postgressService/build/libs/elastic.apm.agent.jar 


test-frontend:
  stage: build
  image: npm:latest
  script:
    - cd frontend/app
    - npm test